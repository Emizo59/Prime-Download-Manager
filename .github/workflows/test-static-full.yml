name: TEST static full (temporary)

# Verifies the FULL SAC recipe: static libtorrent + static OpenSSL, and asserts the
# exe imports NO libtorrent/libssl/libcrypto DLL. Delete once green.

on:
  workflow_dispatch:
    inputs:
      source_ref:
        description: 'Branch/tag in private source'
        required: true
        default: 'main'

permissions:
  contents: read

jobs:
  probe:
    runs-on: windows-latest
    timeout-minutes: 60
    defaults:
      run:
        shell: pwsh
    steps:
      - name: Checkout private source
        uses: actions/checkout@v4
        with:
          repository: ${{ secrets.SOURCE_REPO_NAME }}
          ref: ${{ github.event.inputs.source_ref }}
          token: ${{ secrets.SOURCE_REPO_TOKEN }}

      - name: Install Qt 6.9.3 (win64_mingw) + MinGW 13.1.0 + Ninja
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.9.3'
          host: windows
          target: desktop
          arch: win64_mingw
          tools: 'tools_mingw1310 tools_ninja'

      - name: Pin CMake 3.31 (FindBoost)
        run: |
          Invoke-WebRequest -Uri "https://github.com/Kitware/CMake/releases/download/v3.31.6/cmake-3.31.6-windows-x86_64.zip" -OutFile cmake.zip
          Expand-Archive cmake.zip -DestinationPath cmake_dl -Force
          $bin = ((Get-ChildItem cmake_dl -Directory | Select-Object -First 1).FullName) + "\bin"
          "$bin" | Out-File -FilePath $env:GITHUB_PATH -Append -Encoding utf8

      - name: Fetch prebuilt libtorrent + boost headers (ci-deps-win)
        env:
          GH_TOKEN: ${{ secrets.SOURCE_REPO_TOKEN }}
        run: |
          New-Item -ItemType Directory -Force -Path thirdparty | Out-Null
          gh release download ci-deps-win --repo ${{ secrets.SOURCE_REPO_NAME }} -p thirdparty-mingw64.tar.gz --output dep.tgz
          tar -xzf dep.tgz -C thirdparty

      - name: Install MinGW OpenSSL (MSYS2) + verify STATIC libs exist
        run: |
          C:\msys64\usr\bin\pacman -Sy --noconfirm
          C:\msys64\usr\bin\pacman -S --noconfirm --needed mingw-w64-x86_64-openssl
          Get-ChildItem C:\msys64\mingw64\lib\libssl*, C:\msys64\mingw64\lib\libcrypto* | Select-Object Name, Length
          if (-not (Test-Path "C:\msys64\mingw64\lib\libcrypto.a")) { throw "NO static libcrypto.a in MSYS2 openssl — need another static OpenSSL source" }
          if (-not (Test-Path "C:\msys64\mingw64\lib\libssl.a"))    { throw "NO static libssl.a in MSYS2 openssl" }
          Write-Host "==> static libcrypto.a + libssl.a present"

      - name: Build STATIC libtorrent archive
        run: |
          $env:PATH = "$env:IQTA_TOOLS\mingw1310_64\bin;$env:IQTA_TOOLS\Ninja;$env:QT_ROOT_DIR\bin;$env:PATH"
          $env:MINGW_DIR = "$env:IQTA_TOOLS\mingw1310_64"
          $env:OPENSSL_ROOT_DIR = "C:/msys64/mingw64"
          powershell -ExecutionPolicy Bypass -File build_libtorrent_static.ps1
          if (-not (Test-Path "thirdparty\mingw64\lib\libtorrent-rasterbar.a")) { throw "static .a NOT produced" }

      - name: Fetch curl
        run: |
          Invoke-WebRequest -Uri "https://curl.se/windows/latest.cgi?p=win64-mingw.zip" -OutFile curl.zip
          Expand-Archive curl.zip -DestinationPath curl_dl -Force
          $dir = (Get-ChildItem curl_dl -Directory | Select-Object -First 1).FullName
          "CURL_DIR=$dir" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8

      - name: Configure app (static libtorrent + static OpenSSL)
        run: |
          $mingw = ("$env:IQTA_TOOLS\mingw1310_64\bin" -replace '\\','/')
          $qt = ($env:QT_ROOT_DIR -replace '\\','/')
          $curl = ($env:CURL_DIR -replace '\\','/')
          $env:PATH = "$env:IQTA_TOOLS\mingw1310_64\bin;$env:IQTA_TOOLS\Ninja;$env:QT_ROOT_DIR\bin;$env:PATH"
          cmake -B build -G Ninja -DBUILD_TESTS=OFF -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_C_COMPILER="$mingw/gcc.exe" -DCMAKE_CXX_COMPILER="$mingw/g++.exe" `
            -DCMAKE_PREFIX_PATH="$qt;$curl" `
            -DOPENSSL_ROOT_DIR="C:/msys64/mingw64" 2>&1 | Tee-Object -Variable cfg
          if (-not (($cfg | Out-String) -match 'libtorrent: STATIC archive')) { throw "static archive not selected" }

      - name: Build app
        run: |
          $env:PATH = "$env:IQTA_TOOLS\mingw1310_64\bin;$env:IQTA_TOOLS\Ninja;$env:QT_ROOT_DIR\bin;$env:PATH"
          cmake --build build --parallel
          if (-not (Test-Path "build\Prime Download Manager.exe")) { throw "exe NOT built" }

      - name: ASSERT exe imports no libtorrent/openssl DLL
        run: |
          $objdump = "$env:IQTA_TOOLS\mingw1310_64\bin\objdump.exe"
          $deps = & $objdump -p "build\Prime Download Manager.exe" | Select-String 'DLL Name:' | ForEach-Object { ($_ -split ':')[1].Trim() }
          Write-Host "Imported DLLs:"; $deps | Sort-Object | ForEach-Object { Write-Host "  $_" }
          $bad = $deps | Where-Object { $_ -match 'torrent|libssl|libcrypto|ssl-3|crypto-3' }
          if ($bad) { throw "exe still imports: $($bad -join ', ')" }
          Write-Host "==> PASS: no libtorrent/openssl DLL imports — fully absorbed into exe"
          if (Test-Path "build\libtorrent-rasterbar.dll") { throw "libtorrent DLL present in build (should be gone)" }
